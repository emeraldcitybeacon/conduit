from django.apps import AppConfig


class ConduitConfig(AppConfig):
    """App configuration for project-level components."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "conduit"
